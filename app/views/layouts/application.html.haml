!!! 5
%html
  %head
    %title Sharing
    = stylesheet_link_tag "application", :medial => "all", 'data-turbolinks-track' => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags

    %body
      - if current_user
        #{current_user.full_name} is logged in.
        = link_to 'Logout', sign_out_path
        %hr

      = yield

